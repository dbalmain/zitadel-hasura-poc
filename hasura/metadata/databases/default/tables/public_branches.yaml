table:
  name: branches
  schema: public
select_permissions:
  - role: user
    permission:
      columns: [id, name]
      filter: {}
  - role: branch-coordinator
    permission:
      columns: [id, name]
      filter: {}
